# White a script which accepts a decimal number from user and converts
# it to hexadecimal.

decimal_number = int(input("Please write a decimal number: "))
print("The hexadecimal form", decimal_number, "is", hex(decimal_number))
